RH124

김기연
010197902430
nsr230@studylab.xyz

수업 및 참고자료들은 다음 링크를 통하여 제공 https://github.com/studylabdoc/text/blob/main/REDHAT/RH124/20231211.txt

리눅스 커맨드 pdf
https://drive.google.com/file/d/1X-g1pHmON007-HY2BpT55B-EwOHO1DNA/view?usp=drive_link

온라인 man
https://man.cx/

github.com -> studylabdoc/text
REDHAT -> RH124 

REDHAT mail : search -> 20231211 
Red Hat virtual classroom training class: 20231211-NA-RH124 (46489450

rol.redhat.com -> VT 사이트 RHLS 

MY VIRTUAL TRAINING CLASSES ->
RH124 - Red Hat System Administration I -> JOIN
1. 리소스 -> E-book 
ko , en 
2. 랩 -> 
workstation -> 콘솔열기
강의 webapp [주피터 랩] -> 오픈 workstation으로 student 계정으로 접근 되어 있는지 확인

월 - 금
10:00 - 18:00 진행
점심시간 한시간 , 한시간30분

RH124 기본 명령어 들로 구성
기본 명령어 set + shell 
RH134 로컬 스토리지 / 네트워크 
RH199  124 + 134 -> 압축 -> RHCSA 자격증 대비과정 

계정 정보
root / redhat
student / student

화장실 -> 엘리베이터 왼쪽
흡연 -> 1층  오른쪽 -> 문 : 주차장
경사진 길 -> 우측 컴포즈 커피 

11:10분 

구글 크롬이 느릴때 -> 파이어폭스 

리눅스란 무엇인가?

작은 범위 : 커널 *   linux = kernel 
큰 범위 : 배포판 kernel + 응용프로그램

iptime -> linux kernel 
vmware ESXI  -> linux

RHEL -> fedora, centos | rocky
fedora -> RHEL -> centos
테스트      공식배포   -> 클론  2021.12.31
fedora -> centos stream-> RHEL
테스트       테스트                 공식배포
fedora -> centos stream-> RHEL -> Rocky
Debian - ubuntu
Suse - suse, opensuse 
한컴리눅스 : 국내에서 만든 배포판
linux
lindow : linux + window 합성어

어느순간부터 IT를 한다?
교양과목 : 
1. 리눅스: 정부에서 사용하는 모든 컴퓨터를 linux로 교체하겠다. windows 7 > win 10
2. 파이썬: 학교 : 초등학생들 학교에서 기본 코딩 스크래치 블록 코딩 

절대 이 두가지는 교양이 아닙니다. 생존 기술

1991. 리누스 토발즈 - 커널을 공개

리눅스 - 공짜 

무료 소프트웨어 != 리눅스
리눅스 -> 오픈소스 

GNU gnu not unix  재단

UNIX : 리눅스 이전에 사용하던 서버
IBM HP SUN[ORACLE]
하드웨어 + 소프트웨어 -> 고가

락인효과로 다른 브랜드 구매가 꺼려지는 상황

배포판 -> 
GNU만의 배포판을 만들어서 UNIX 타도를 외치겠다.

응용프로그램의 작성은 순조로웠다.
커널 -> Hurd : 메모리 누수
1991 리누스 토발즈 -> 커널 배포
GNU - 리누스 토발즈 -> linux kernel GNU 

공식적으로 배포판이 출시

고소장의 축제

Minix -> LINUX 
LINUX - Hurd +  LINUX  + Minix -[UNIX]
-> LINUX 2.0 

REDHAT  무료소프트웨어 인식이 강한 사이에서 어떻게 살아남았나?

redhat 처음 회사를 창립 
서브스크립션 -> 유료 판매 

사람 반발: 리누스 토발즈 : 돈을 받고 파는것도 자유다.
redhat : 우리 제품을 사용한다면 [서브스크립션을 구매한다면?] 고소에 대한 리스트는 본인들이 다 책임지겠다.

cisco : IBM 납품 워런티 : 2년 라이프타임으로 해주겠다.

redhat -> redhat linux  > RHEL  2010년도 쯔음...

IBM
redhat 

인터페이스
GUI 그래픽 모드
CLI  텍스트 모드 

최근의 인프라에서 두가지
1. 자동화 : 어떠한 작업에 대하여 사람이  직접 건드리지 않고도 수행이 가능한 상태
2. 코드화  : 특정한 언어를 대상으로하여 자동화를 위한 모드를 작성 yaml* , toml * , python *, perl

기본 명령어
------------>
리눅스 기본 명령어.
70개
PDF 파일의 경우에는 옵션이 빈약.
옵션들은 제가 따로 추가를 해 드릴예정
시간이 부족한 관계로 필수만 진행.
나머지는 글자만 붙여넣고 필요한 경우에 진행.
https://en.wikipedia.org/wiki/Protection_ring


shell
sh - ksh - [bash] - zsh 
        csh - tcsh 
쉘? 

$ :  관리자 계정을 제외한 나머지 계정
UID가 0이 아닌 경우
UID != 0 = non root 

# : 관리자 계정 root
UID가 0 인경우

UID =0 = root 

계정 생성
계정 생성
비밀번호 구성
첫 로그인시 비밀번호 변경
생성된 계정으로 메일을 전송

쉘을 구성하는 3요소
# [1] [2] [3]

[1] 명령어 : 리눅스의 명령 : 실행
쉘에서 처음 오는것은 무조건 명령어
명령어로의 구분? 빈칸[white space] -> 단위 토큰
존재하는 명령어가 실행대상이 된다.

[2] 옵션 : 명령의 기능 집합 : 조정
하나의 명령어 안에는 보통 수가지의 옵션이 존재.
이를 확인하기 위해서는 매뉴얼을 확인
man ls , ls --help 
두가지 형태의 옵션
# CMD o
# CMD -o
# CMD --option
-형태와 --형태를 둘다 제공하는 옵션도 존재하지만 
--만 존재하는 형태도 존재.  -만 있는 것도 가끔 있음.

[3] 인자 : 파일, 디렉토리로 지정 : 타겟 
현재 디렉토리가 아닌 특정한 디렉토리나 파일을 대상으로 명령어를 실행하는 경우에 사용 

EX: ls -l /var
$ ssh root@servera

옵션과 인자.
# CMD -a
# CMD --all
# CMD -ai
# CMD -a -i
# CMD --all -i

만약에 옵션에 인자가 포함되어 있는 명령어라면 :
옵션과 인자를 연속하여 사용해야 한다.
# find / -arg A -arg2 B -arg3 C : 성공
# find / -arg A -arg2 C -arg3 B : 명령어 실패 

로그인 환경:
CLI : 텍스트 환경 -> 문자로만 이루어져 있다.
GUI : 그래픽 환경 -> 이용하기 편한 형태, mem를 많이 사용.
Connection to servera closed by remote host.

원격 로그인 환경
[user@host ~]$ ssh remoteuser@remotehost
ssh          remoteuser@ remotehost
명령어     계정      구분자  호스트네임 
만약에 계정을 입력하지 않고 접근하면 현재 호스트의 계정이 기본값으로 사용
remoteuser@remotehost's password: password
[remoteuser@remotehost ~]$
#

컴퓨터의 3대 동작
입력-연산-출력


명령어의 형식
1. 명령어 자체로 실행되고 결과를 알려줌.
2. 명령어를 실행시 입력을 대기하고 입력된 내용을 기반으로 실행되는 명령어.
3. 명령어 실행시 명령어 내부로 진입 작업이 끝나고  종료 명령을 내렸을때 쉘로 복귀.

# man 
매뉴얼
리눅스에도 모든 프로그램에 매뉴얼 
# CMD --help
# man CMD
man 페이지 내에서 페이지 제어
<Enter> 하나의 라인씩 밀리면서 출력한다.
<Space> <--> <CTRL + B> 한 페이지씩 밀리면서 출력한다.
      CTRL + B로 이전 페이지로 돌아간다.
/-a     <--> n, N    (n: next) 특정 단어를 검색하여 찾는다. 두 개 이상 존재할경우는 n으로 다음 단
    어를 찾는다 
: 자주 사용되는 기능
/OPTIONS 
/EXAMPLES  지금은 대소문자 구별없이 검색이 가능.
q 
h
# man man
섹션번호 
매뉴얼 페이지 각 섹션별 정보
  1     누구나 실행할 수 있는 사용자 명령 (User Commands)
  2     시스템 호출 (System Calls), 즉 커널에서 제공하는 함수
  3     C 라이브러리 함수 (C Library functions)
  4     디바이스와 네트워크 인터페이스 (Devices and Network Interfaces), 즉 /dev 디렉토리에 있는 특별한 파일들
  5     파일 포맷 (File Formats), 예를 들어 /etc/passwd
  6     게임과 데모 (Games and Demos)
  7     환경, 테이블, 매크로 (Environments, Tables, and Macros) 등의 잡다한 여러 가지들
  8     시스템 관리자만 실행할 수 있는 시스템 관리용 명령들 (Maintenance Commands)
  9     커널 루틴에 관련된 문서들의 리눅스만의 독자적인 저장소
  n     새로운 문서들의 저장소. 보다 적당한 장소로 이동된다.
  o     오래된 문서들의 저장소. 유예 기간으로 보관되고 있는 것들이다.
  l     특정 시스템에 대한 로컬 문서들
# man passwd 실행 파일에 관련하여
# man -s 5 passwd 파일의 포맷에 관련하여 

# man -k password
password: nothing appropriate.
키워드 검색이 정상적으로 되지 않는다. : 인덱스 파일이 아직 만들어 지지 않았다.
# mandb    : 6버전미만에서는  makewhatis 
# man -k password
...

지금은 이용하지 않음.
# info # pinfo   

# whatis ls -> 섹션번호 확인, NAME 섹션의 짧은 설명 
# whereis ls -> 도움말 파일 위치 확인
# which  ls -> 명령어의 절대 경로 (실제 파일의 위치)

# pwd
현재 작업 디렉토리 확인

# cd
작업 디렉토리 변경
# cd [내가 가고싶은 디렉토리]
이동하려는 곳 
/test/one/two
현재 위치
/test/one
1. 절대 경로
기준점: / 출발점이 항상 동일하다. 그래서 틀릴 일이 없다. 
이동하는 방법
# cd /test/one/two
: 디렉토리 위치가 파일의 위치가 가까운경우 
 
2. 상대 경로
기준점: 현재 디렉토리 # pwd
이동하는 방법
디렉토리를 대상으로 이동
# cd two

/test/one/two
1       2     3

1 -> 최상위 디렉토리
23 -> 디렉토리 구분자. 

모든 디렉토리에 존재하는 파일
.    : 현재 디렉토리
..   : 현재 디렉토리의 부모디렉토리 

디렉토리의 위치가 13단계 
작업하려는 위치 -> 12단 11단계

# cd -   :  바로 직전에 이동한 디렉토리로 변경


# ls *
디렉토리 내의 파일 확인
여러가지 옵션이 있지만 다음은 꼭 알고 갑시다.
-a : 모든 파일 표시, 여기에는 숨김 파일(점(.)으로 시작하는 파일)도 표시한다.
-l : 디렉토리가 지정되는 경우 디렉토리의 내용을 자세히 보여준다. 또한 파일의 내용 이 지정되는 경우 파일의 속성 정보를 자세히 보여준다. 파일 종류, 링크 수, 소유자명, 그룹명, 파일 크기, 최종 수정일 및 용량, 파일명 표시한다.
-R : 해당 디렉토리와 서브디렉토리의 모든 내용을 표시
-F : 디렉토리인 경우에는 디렉토리 "/"표시를 하고 실행 파일인 경우는 뒤에 "*"표시를 한다.
-i : 해당 파일의 inode 번호를 표시한다.
-n : 파일의 소유자와 그룹을 숫자로( UID : GID )표시한다.
-d : 찾고자 하는 디렉토리에 관한 정보만을 표시한다.
-h : 사람이 보기 좋은 형태로 용량을 변경
-v : 숫자에 대한 정렬을 실시.

# ls -l
total 0
-rw-r--r--. 1 root root 0 Dec 11 01:20 file1

- 파일의 형식
- 파일 : 리눅스에서는 왠만한 것들은 그냥 - 로 표현 
d 디렉토리 폴더=디렉토리
b 장치
c 장치
l 링크 (바로가기)

rw-r--r-- 권한 rwx rwx rwx 
. : ACL .= acl 정보가 없다. += acl정보가 있다.
setfacl / getgacl 

1 링크숫자 : 파일시스템내에서 동일한 파일로의 포인트가 몇개가 있는가? 
root 소유자 문자열:숫자
root 그룹소유자 
0 : 파일의 용량
Dec 11 01:20 시간 :수정시간
stat
Access: 접근 시간
Modify:  수정시간 : 데이터에 대한 변경
Change:  변경 시간 : inode data의 변경

file1 : 파일명 

# passwd
# useradd
# rm
삭제
주의사항: CLI에서 하는 모든 작업은 되돌릴수 없다.
GUI의 터미널 ,  원격 ssh , 콘솔 모든걸 모함
삭제를 하는 순간 되돌릴수 없다.
rm의 경우에는 다음과 같은 옵션을 가지고 있다.
rm -i : alias 에 기본 등록되어 있음. 응답형으로 동작.
rm -r 디렉토리 삭제
rm -v 삭제되는 파일을 출력
rm -f  삭제시 묻지 않음.
디렉토리 삭제 -> rm -rf 

# cp
복사 
# cp 원본 타겟
# cp file1 file2
-p 속성을 복사. 권한 소유자 시간 
-r 디렉토리 내의 파일을 같이 복사하는 경우
-i alias 에 기본 등록되어 있음. 응답형으로 동작.
-v 복사되는 파일을 출력: 자세히 보기
# cp file1 file2 file3 file4 file dir1

# mv
이름 바꾸기 , 파일 이동
# mv file1 file10
# mv file1 /dir1
# mv file1 file2 file3 file4 file5 /dir1
# mv dir dir2
# mv dir1 dir2 -t dir3 
# mv dir dir1 : dir1이 존재하는 경우에는 dir이 dir1으로 종속 

# date
시간을 확인
date 명령은 시간 포맷을 변경하는것이 중요.
# date +%m%d_%H%M
# date +%option 
시간 포맷을 변경하는 것 자체 포인트

# touch
1. 빈파일 만들기
2. 파일의 시간 변경하기
# touch -t 08111300 file10 ; ls -l file10
# stat file10
Access , Modify의 나노초가 00000000으로 구성되어 있으면 시간이 조작된 파일이다.

# mkdir
디렉토리 생성
-p 부모디렉토리가 없는 경우에 같이 생성

# rmdir
빈 디렉토리를 삭제하도록 설계 
.와 ..을 제외한 그 어떤 파일도 존재하면 안된다.
디렉토리 삭제
-p 부모 디렉토리를 같이 삭해야해야 하는 경우에 삭제
내부에 파일이 있는 경우에는  rm -rf 를 사용

# id
계정 정보를 확인

# cat
텍스트 파일을 읽고
파일을 병합하고
파일을 표준출력으로 확인

# ps
프로세스를 확인하는 명령

# bash
쉘

# su -
사용자 전환

# echo
# more
긴 파일을 페이지 단위로 보여줌.
사용법은 man과 동일
 
# man
매뉴얼
# clear
화면을 깨끗하게 청소

# ln
링크 
하드링크/ 소프트링크
inode와 파일간 링크 두가지를 생성 가능한 ln 명령

하드링크 - 디바이스[inode] ln file1 file2
소프트링크 - 파일시스템[FILE PATH] ln -s file1 file2

둘다 원본이 되는 파일이 존재해야 함.
하드링크
# ln file1 file2
inode count가 +1 되는것을 확인 
링크 명령을 통하여 생긴 파일의 inode는 동일 
inode 가 동일하다 =
1. 권한이 똑같음
2. 소유자와 그룹소유자가 동일
3. 용량도 동일함
4. 수정시간이 동일

파일 = 인터페이스
실제 디스크, 메모리에 담겨 있는 컨텐츠. 보기위한 추상화된 인터페이스

                             SHELL ----> FILE
------------  
USER                   
-----------                        VFS---? inode table 
KERNEL
---------
H/W                                              file1-data 

하드링크를 생성한다고 해서 디스크의 파일이 또 하나 생기는 것은 아니다.
원본의 개념이 따로 없음. 생성되는 링크마다 모두 원본

소프트링크
# ls -li
total 4
25606376 -rw-r--r--. 1 root root 692257 Dec 11 20:22 file1
25606381 lrwxrwxrwx. 1 root root      5 Dec 11 20:20 file2 -> file1
1. inode 번호가 다름. 전혀 다른 파일
2. 권한부분이 rwxrwxrwx 모두 채워져 있다. 상관 없음. 실제로는 원본파일의 권한을 사용하기 때문에 링크의 권한을 수정 하지 않음. 
3.  원본 파일이 아무리 커도 링크 파일의 용량은 커지지 않다.
4. 파일의 생성 시간도 다름.
5. 파일의 보이는 모습도 다름.
6. 파일 형식이 l로 표현

링크 파일의 경우 inode가 담겨 있지 않고 파일의 PATH [실제 경로]가 담겨 있다.
이동형 디스크의 경우에 링크를 사용하는 파일에서 생기는 문제점.
# ln -s /test1/file1 file2
심볼릭 링크를 생성할 때 원본 경로는 절대 경로로 주시는게 좋음.!

차이점 및 한계점
하드링크
디렉토리 링크 :
ln dir1 dir2
ln: dir1: hard link not allowed for directory 하드링크에서는 디렉토리에 링크를 걸 수 없다.
다른 파일시스템에 링크 걸기
# ln file1 /boot/file1
ln: failed to create hard link '/boot/file1' => 'file1': Invalid cross-device link : 파일시스템은 달라도 inode 번호가 동일하게 사용되는 경우가 있다.  파일시스템에 다르면 하드링크는 불가능.

소프트링크
디렉토리 링크 : 가능
다른 파일 시스템에 링크 걸기 : 가능

하드링크 : 하드웨어 기반 inode
소프트링크: 소프트 기반 file path

87p  랩 
 
118p 이동
도움말 
VI 


# tree
디렉토리의 전체 트리상태 

# dnf  소프트웨어 설치 tool 
# chmod 권한 변경
# chown 소유자 변 경
# umask 기본 권한 변경
# which 
# whereis
# alias 명령어를 짧게 약어 형태로 사용
# histroy 사용된 명령어를 확인
# stat 시간정보에 대하여 자세하게 확인
# file 파일의 형식
# cat file1 file2 > file3  표준 입출력 
# find . -name core 2> /dev/null 표준 입출력 
# cat < file1 표준 입출력 
/proc/PID/fd
0
1
2

fd에서 이미 예약되어진 번호 3가지가 있으며 0, 1 ,2 번이다. 그리고 이는 다음과 같이 부른다.

stdin 표준 입력

stdout 표준 출력

stderr 표준 에러

표준 입출력에 대한 방향 전환

입력
<
# CMD 0< file
# CMD < file  * 
 
출력
>     덮어쓰기
>>   이어쓰기
1 2 
표준 출력
# CMD 1> file
# CMD 1>> file

# CMD > file
# CMD >> file

표준 에러
출력과 에러의 차이
$?  < 실행한 명령어의 성공 값
0/ 1-255

# CMD 2> file
# CMD 2>> file
표준 출력은 기본값으로 1을 사용하고 생략이 가능하지만 표준 에러는 생략이 불가능.

하나의 파일에 표준 출력 / 에러를 동시에 담기
# CMD > file1 2>&1
표준 에러를 표준 출력의 스트림으로 변경

161p 랩 25 
40분
표준 입력 /  표준 출력

표준 에러 > 원본으로 넘기는것 * 














# ssh 원격 명령어 세트 
# scp 원격 명령어 세트 
# sftp 원격 명령어 세트 
# script 화면에 대한 로깅 정보
# vi 텍스트 에디터
운영체제 ->

입력에 대한 기능 
텍스트 에디터 -> 입력 / 수정 / 출력

운영체제ED --->UNIX -> LINUX

VI
VIM

VI의 세가지 모드
기능모드: vi 자체의 기능을 이용하기 위한 모드, 이동, 복사, 삭제, 치환..
입력 모드 : 글자를 입력하여 텍스트를 수정하거나 데이터를 입력
최 하위행 모드: 저장, 불러오기 ,빠져나가기, 검색 + 명령어 실행 

기능 모드
: vi에 진입을 하는 순간 기본 모드 
이동 : 방향키 , HJKL 
삭제 : x #x   #=반복할 숫자. 
x : 한 문자 삭제
#x: 연속된 문자 삭제
dw: 워드 삭제 
#dw : 연속된 워드 삭제
dd : 라인 삭제

치환: 
r : 한문자 치환
R : esc키를 누르기 전까지 계속  전환


복사
yy
붙여넣기
p
해당 라인에서 바로 붙여넣기 하면 라인별로 내려가지만 블럭을 벗어나서 붙여넣기를 하게 되면 세트로 붙여넣기 가능.

되돌리기
u
되돌리기 취소
ctrl+r



입력 모드
: 특정 기능키를 이용하여 입력 모드로 전환.
iI oO aA 
i A o 
i : 현재 커서가 밀리면서 입력 시작
A : 현재 커서가 있는 라인의 마지막으로 이동하여 입력 시작
o: 현재 커서의 라인 아래에 새로운 라인을 생성하고 입력 시작
입력모드에서 esc를 이용하여 기능모드로 전환 

최 하위행 모드:
: 특정 기능키를 이용하여 최 하위행 모드로 전환
: / 
enter,, esc 키를 이용하여 기능모드로 전환 

:w  저장
:w file1  file1이라는 파일명으로 저장
:q 빠져나가기
:wq 저장후 빠져나가기
:w!   : != 강제의 의미를 가지고 있으며 w! 강제로 저장.
:wq! 
:#   #에 해당하는 숫자의 라인으로 이동
gg : 제일 윗줄로 이동
shift + g : 제일 마지막줄로 이동

: set nu : 라인번호 설정
: set nonu : 라인번호 해제
: set ts=2  탭문자 간격 설정
: syntax off : 글자의 색깔 변경 해제


12:30 ->  14:00
124 
:!CMD 명령어를 vi를 빠져나가지 않고 사용

vi에서 대소문자 구별없이 검색하기
/검색어 : 대소문자 구별
/\c검색어 : 대소문자 구별없이 검색

지역 변수
현재 쉘만 적용
# 변수명=변수값
# var=50
# echo $var
50
# set 

환경 변수
현재 쉘 + 서브쉘 적용
# export var
# export var1=60

해제하기
# unset var    : 변수 자체를 해제
# export -n var : 환경변수에서만 해제 -> 지역변수로는 남아 있다.


set : 현재 쉘 + 서브쉘
env : 서브 쉘 

RH124 + RH134 
RH199 -> EX200  RHCSA





기본 기능 키 

# systemctl systemd 제어 명령어
# exit 쉘을 빠져나가거나, exit code를 작성할때 사용
# export 환경 변수 적용
# ^c : 기존 작업을 강제 종료 
# ^d : EOF , exit 기능
# poweroff 
# reboot
# shutdown

# ping
# telnet
# ifconfig

# head
텍스트 파일의 첫 부분을 확인
기본값 10줄
# head -1 file
# cat file | head -1

# tail
텍스트 파일의 끝 부분을 확인
기본값 10줄
# tail -1 file
# cat file | tail -1
-f 로깅 파일의 추가되는 데이터를 확인하는 용도로 사용
-F 5M이상의 파일 지속적인 모니터링에 이용 

# who
# last
# lastb : 로그인 실패 계정에 대한 로깅
# w
# while true do ; action ; done

# df
# du
# sort
# tee
# screen
# mail
# wall 
# grep
# find


mk : 생성
rm : 삭제 
ch : 변경 
ls : 확인

CMDset
	ㄴ 1set
		ㄴ 명령어..
	ㄴ 2set
		ㄴ 명령어..


사용자 / 그룹 사용자

계정생성 -> 삭제

그룹 생성 -> 삭제

비밀번호 관리 /비밀번호의 생애주기
비밀번호를 관리자 변경 -> 일반 사용자에게 넘겨주는 태스크

# id
uid=0(root) gid=0(root) groups=0(root) 
uid -> 현재 계정의 UID 숫자된 ID : 컴퓨터가 인지하는 계정에 대한 코드 
gid -> 현재 계정의 GID 숫자된 ID : 컴퓨터가 인지하는 그룹계정에 대한 코드 
groups -> 현재 계정이 포함된 여러 그룹의 집합.

id -> password 일종의 인증 체계를 거쳐야 로그인이 완료 된다.

관련된 파일
/etc/passwd
/etc/group
/etc/shadow
/etc/gshadow
+
/etc/passwd-
/etc/group-
/etc/shadow-
/etc/gshadow-
뒤에 -가 있는 파일들은 기존 파일이 업데이트가 되면 원본 파일을 백업파일로 이동시킨후 업데이트 

useradd 계정을 생성 
 cat /etc/default/useradd
# useradd -D
GROUP=100
HOME=/home
INACTIVE=-1
EXPIRE=
SHELL=/bin/bash
SKEL=/etc/skel
CREATE_MAIL_SPOOL=yes

usermod
# usermod -aG g1 team01
그룹을 추가하는 경우에는 -aG 로 기존 그룹을 보존한 상태로 그룹을 추가해야한다.

userdel
# userdel -r user
계정을 삭제할 때 -r 옵션을 주지 않는다면 파일이 남기 때문에 조심해야 한다.

passwd
#
# passwd
# echo text | passwd --stdin account
# echo 계정:비밀번호 | chpasswd


https://systemd.io/UIDS-GIDS/


그룹 관리

사용자 1..10000

하나의 그룹으로 관리하기 위한 목적의 그룹
> 권한 

그룹에다가 권한을 준다.
사용자 1-10000까지 권한을 부여한다.

인적 오류가 확률이 적어진다.

# groupadd group1   < gid를 따로 지정하지 않았기 때문에 마지막 사용된 GID +1이 사용된다.

1000 1000
            1001
1001  1002 
1005 
1. # groupadd -g GID g1
2. /etc/login.defs

UID_MIN  GID_MIN   을 사용하지 않은 번호로 재설정


init
0  파워오프
1 싱글
2 멀티 nfsX
3 멀티
4 사용하지 않음.
5 멀티 + 그래픽
6 재부팅
SAH-512 + password + salt
1

SAH-512 'redhat' + bj23jht4 = 45Jc6H30E
2
SAH-512 redhat + shtfsdf= hUfzfHpc9X


# chage -d 0 계정
원래 -d 옵션의 목적은 비밀번호를 업데이트한 날짜를 변경하기 위함이지만 -d 옵션에 인자로 0를 사용하게 되면 강제로 다음 로그인시 비밀번호를 변경하게 끔 설정

사용자가 접근을 했다 : 비밀번호는 사용자만 알고 있다. 관리자이지만 계정이 해킹을 당해도 책임에서 회피가능

처음 계정을 생성시 해야하는 리스트

1. 계정 생성
# useradd team01
2. 비밀번호 구성
# passwd team01

echo로 비밀번호를 변경하는 경우. or 이런 작업을 하면서 남기기 싫은 경우
# bash
# history -c
# exit


3. 필요한 나머지 구성
# usermod -aG group team01
# ....
4. 사용자가 처음 접속시 비밀번호를 변경하게끔 설정
# chage -d 0 team01

파일에 대한 액세스 제어
chmod chown : 
bash shell 
find / grep 

 파일의 속성 정보 확인
 파일의 속성 정보 변경
 파일의 소유권한 / 그룹권한 변경
 파일의 퍼미션 변경
 파일의 특수퍼미션 변경

파일의 속성 정보 변경 명령어에는 chown, chgrp, chmod, umask와 같은 명령어 등이 있다.

# ls -l file1
-rw-r--r-- 1 root root 1945  6월 11 14:13 file1

File Type: X
Permission Mode: chmod
Link Count: ln 
Owner: chown
Group: chgrp , chown
File Size: 
Mtime: touch -t
File Name: mv

파일의 소유권한 / 그룹권한 변경

chown 명령어

chown 명령어는 Unix 계통 시스템에서 파일의 소유권을 바꾸기 위해서(change the owner of a file)사용된다. 대부분의 경우, 이것은 오직 슈퍼 사용자에 의해서만 실행될 수 있다. 그들이 소유하고 있는 파일의 그룹을 바꾸고 싶어하는 비특권화된 (일반적인) 사용자들은 chgrp을 사용해야 한다.

[명령어 형식]
# chown user01 file1
# chown user01.other file1  (# chown user01:other file1)
# chown user01:other file1 
그룹의 경우에 .이나 :을 앞에 찍어 그룹임을 명시.
권장사항은 :으로 사용하는 것.
# chown .other file1
# chown :other file1
# chown -R user01 dir1
# chown -R user01:other dir1

-c : 바뀌어지는 파일들에 대해서만 자세하게 보여준다.
-f : 바뀌어 지지 않는 파일들에 대해서 오류 메시지를 보여주지 않는다.
-v : 작업 상태를 자세히 보여준다.
-R : 경로와 그 하위 파일들 모두를 바꾼다.

chgrp는 일반 사용자가 이용하는 그룹을 변경하는 명령
조건 2
1 해당 파일의 소유자 일것
2 변경하려는 그룹에 현재 계정이 가입 되어 있을 것
두 가지 조건을 모두 만족해야 일반 사용자가 파일을 변경 할 수 있다.


chmod
권한에 대한 변경
  rwxrwxrwx * 
 chmod 명령어

파일이나 디렉토리를 새로운 권한으로 변경하는 명령이다. 파일의 소유자나 관리자만이 chmod를 사용할 수 있으며 파일의 소유자, 파일의 그룹, 다른 사용자로 나누어 각각의 권한을 설정 할 수 있다.
# ls -l file1 
-rw-r--r-- 1 root root 0 Jan 10 12:43 file1

[명령어 형식]
# chmod u+x file1  심볼릭
# chmod 755 file1  옥탈

[1] 심볼모드(symbolic mode)를 이용한 권한 변경
RWX RWX RWX
----- ------ -----
  U         G        O

[사용자 기호]
기호                       설명
u      user           파일/디렉토리의 소유자
g      group          파일/디렉토리의 그룹
o      other          다른 사용자 
a      all            소유자, 그룹, 다른 사용자 모두(아무 표시 안할 경우 기본적으로 설정됨)

[설정 기호]
기호                        설명
+       퍼미션 허가       지정한 퍼미션을 허가한다.
-       퍼미션 금지       지정된 퍼미션을 금지시킨다.
=       퍼미션 지정       지정한 퍼미션만 허가하고 나머지는 금지 시킨다.
                       설명 : 이전에 권한을 어떻게 줬던 지금 설정해주는 권한으로 대체시켜서 사용 가능
                       (+,-는 내가 이전에 어떠한 권한을 설정해 줬는지 확인해야 하지만 =는 이전에 어떻게 설정하였던 상관없이 설정이 가능한 것이다.)

[권한 기호]

r        w        x
read write execute

설명 : 읽기 권한이 없으면 파일 안에 있는 내용을 볼 수 없다. 따라서 파일을 수정하기 위해서는 반드시 read권한을 부여해야지 파일을 열어서 보고 수정 할 수가 있는 것이다. 만약에 write권한만 있다면 파일을 열어 볼 수 없으므로 수정이 불가능 한 것이다. 따라서 파일을 수정하려면 read&write권한이 모두 있어야 한다.

심볼의 정의
u(User)
g(Group)
o(Other)
a(all)

+(Add)
-(Deny)
=(equal)

r(Read)
w(Write)
x(Excute)

심볼 모드를 사용한 퍼미션 변경

# cd /test 
# rm -rf /test/* 

# touch file1 
# ls –l file1
-rw-r--r-- 1 root root 0 Feb 14 11:12 file1

# chmod u+x file1
# ls –l file1
-rwxr--r-- 1 root root 0 Feb 14 11:12 file1*

# chmod g-r file1
# ls –l file1
-rwx---r-- 1 root root 0 Feb 14 11:12 file1*

# chmod u-x,g+x file1
# ls –l file1
-rw---xr-- 1 root root 0 Feb 14 11:12 file1*

# chmod a=rwx file1 
# ls –l file1

-rwxrwxrwx 1 root root 0 Feb 14 11:12 file1*

파일과 디렉토리 퍼미션의 정확한 의미
F
R  파일을 읽을 수 있는 권한
W 파일을 수정 할 수 있는 권한
X 파일을 실행 할 수 있는 권한

D
R  디렉토리의 컨텐츠를 읽을 수 있는 권한 : ls 
W 디렉토리의 컨텐츠에 수정을 가 할 수있는가? : touch cp rm ..
X  디렉토리에 진입 할 수 있는 권한 : cd 

옥탈모드 :
각 권한마다 할당 되어 있는 숫자가 존재
R W X
4  2  1

소유자권한비트  그룹권한비트  기타권한비트
r  w  x     r  w  x    r  w  x
1  1  1     1  1  1    1  1  1
4  2  1     4  2  1    4  2  1
파일 소유자 권한 : 400 = 읽기 권한, 200 = 쓰기 권한, 100 = 실행 권한
그룹 사용자 권한 : 40 = 읽기 권한, 20 = 쓰기 권한, 10 = 실행 권한
기타 사용자 권한 : 4 = 읽기 권한, 2 = 쓰기 권한, 1 = 실행 권한

--- : 권한 없음 0           000
--x : 실행 권한 1           001
-w- : 쓰기 권한 2           010
-wx : 쓰기 실행 3           011
r-- : 읽기 권한 4           100
r-x : 읽기 실행 5           101
rw- : 읽기 쓰기 6           110
rwx : 읽기 쓰기 실행 7       111

F 기본 권한 R 
0  --- O
1 --x  X
2 -w- X 
3 -wx X
4 r--  O
5 r-x  O
6 rw- O
7 rwx  O

D 기본 권한 X
0  ---  O
1 --x  X
2 -w- X
3 -wx X
4 r--   X
5 r-x  O
6 rw- X
7 rwx O

# chmod 754 file1
# chmod u=rwx,g=r-x,o=r-- file1

권한 변경 실습에 사용하면 좋은 명령어
# watch -n 1 'CMD'

U G O 
기본 권한 체계

그럼 이를 넘어서는 권한이 필요한 경우?

파일. 디렉토리에 소유권을 잠시 빌려야 하는 경우.

 SetUID / SetGID/Stikybit
      F                D             D
파일에 대한 소유권을 잠시 다른 사용자에게 빌려 줌으로 인해 소유권이 없는 사용자가 잠시 동안 파일에 대한 소유권으로 권한을 행사 할 수 있는 것을 말한다.

공유한 디렉토리나 파일에 대한 특별한 퍼미션을 부여하는 것으로서 파일 소유자(owner)나 superuser만이 파일에 대해서는 setuid와 setgid를 설정하고,디렉토리에 대해서는 sticky-bit 퍼미션을 설정 할 수 있다. 

absolute mode (octal mode)나 symbolic mode를 사용하여 지정하거나 해지할 수 있다.

file1 A
B 이용 > 파일을 이용하는 중에 프로세스 -> B가 소유한 프로세스
file1 -> file2

EX: passwd

passwd : /etc/passwd, /etc/shadow 


유저 권한을 빌리는 경우
소유자로 잠시 프로세스를 실행하고 소유자처럼 실행

그룹 권한을 빌리는 경우
그룹소유자로 잠시 프로세스를 실행하고 그룹소유자처럼 실행

디렉토리에 설정
디렉토리로 진입하는 경우에 그룹을 자동으로 스위칭을 하거나, 내가 생성한 파일 이외에는 수정이 불가능.

# chmod []### F|D
# chmod #+s
# chmod o+t D
setuid
# chmod 4### file1
setgid
# chmod 2### dir
stikybit
# chmod 1### dir

setuid -> 파일의 소유자로 실행하여 2차 파일을 접근할때 
setgid -> 공유 디렉토리
setgid 해제시에는 chmod 00### 권한을 주어야 setgid가 해제가 된다.

권한자리     KSUGO

umask 

파일과 디렉토리는 기본권한을 가지고 있음.

F
666 rw-rw-rw-

D
777 rwxrwxrwx


  666 777  기본권한
- 022 022  umask
-------------
  644    755 

umask를 조절하면? 내가 원하는 파일의 권한을 설정할수 있다?

666
022
002 ?

umask를 조절하여 파일을 생성할때 원하는 권한을 미리 부여 할 수 있다.
~/.bashrc 파일에
umask ### 을 부여하여 접속시마다 따로 설정 하지 않아도 된다.

# umask   : 현재 umask를 확인
# umask ###   : 현재 쉘에서 umask를 임시적으로 변경

umask의 숫자의 함정에 빠지지 말자.
umask 003
touch 003
umask 004
touch 004
umask 005
touch 005
7 - 5 4 3
-rw-rw-    [r--]. 1 root root 0 Dec 13 00:30 003 4 r--
-rw-rw-    [-w-]. 1 root root 0 Dec 13 00:30 004 3 -wx
-rw-rw-    [-w-]. 1 root root 0 Dec 13 00:30 005 2  -w-

3으로 빠진 권한의 경우 원래 대로라면 6-3 -> -wx 가 나와야 정상이지만 실제로는 -w- 로 노출된다. 이유는?

숫자지만 숫자가 아니기 때문이다.
umask의 숫자는 숫자가 아니라 권한의 매핑
6-3 X
rw- - -wx =  r--

4 = r
3 = wx
2 = w 
0 = ---
7 = rwx ...
01234567 

F
rw-rw-rw- g-w,o-rwx   rw-r-----
D 
rwxrwxrwx  g-w,o-rwx rwxr-x---

chown / chmod / umask는 세트 

리다이렉션(Redirection)
파이프(Pipe)
배쉬쉘 기능(Bash Shell Funtion)
변수(Variable)
메타캐릭터(Metacharcter)
히스토리(History)
환경 파일(Environmnet File)

쉘은 명령행에서 입력되거나 스크립트 파일에서 읽어 들이는 명령어들을 해석함으로써 사용자와 커널의 중계(Interface) 역할을 담당한다. 사용자가 로그인하면 쉘이 시작되고 사용자가 명령어를 입력하면 쉘은 명령행의 구문을 분석하고, 리다이렉션, 파이프, 와일드 카드, 작업 제어등을 처리하고, 명령어를 PATH변수에서 검색하고 존재하면 실행한다. 쉘의 주요 기능들 중 하나는 명령행 프롬프트에서 입력된 명령어들을 해석하는 대화형 기능이다. 쉘은 명령행에서 입력된 명령어들을 분석해서 '토큰'이라는 단어 단위로 해석한다. 토큰은 탭이나 빈칸, 개행문자 등 [공백]으로 구분한다. 입력된 명령어들에 메타문자들이 포함되어 있으면 적절하게 처리한다. 쉘은 파일 입출력과 백그라운드 작업을 처리하며, 명령행에서 입력이 정상적으로 분석되었을 경우 해당 명령어를 찾아 실행한다.

쉘의 기능을 요약해 보면 다음과 같다.

- 쉘은 PATH 변수에 정의된 모든 디렉토리를 참고 하여 명령어를 찾아 실행한다.
- 쉘은 파이프, 입/출력 리다이렉션, 백그라운드 프로세싱(Background Processing)을 설정한다.
- 쉘은 명령어의 모임인 별칭(alias), 쉘 함수(Function) 등을 찾아 실행시킨다.
- 쉘은 TERM 변수를 사용하여 서로 다른 터미널 환경을 초기화 한다.
- 쉘은 명령어 자동 완성 기능을 사용하여 입력한 명령어를 기록한다.
- 쉘은 사용자 환경 정의 파일을 가지고 사용자의 환경을 초기화 한다.

- 명령어 해석기(Command interpreter)
- 프로그램 언어(Programable Language) ---> 쉘 스크립트(쉘 프로그램)

# 리다이렉션

- 파일기술자
/proc/PID/fd/
각 프로세스를 위한 파일 기술자(File Descriptor)가 쉘에 의해 만들어 진다. 한 개의 파일을 열면 그 연 파일을 나타내는 특별한 숫자를 쉘이 만들어 내는데 이것이 파일 기술자이다. 이런 번호들 중 미리 예약 되어져서 파일을 열 때 주어지지 않는 번호가 있는데 아래 표와 같다.

0
1
2
방향 재지정 (Redirection)

예약되어진 파일 기술자(File Descriptor)

파일기술자 : 축약의미 :설 명
0 : stdin : Standard Input, 표준입력, 특별히 입력이 지정되지 않은 경우 키보드에서 입력을 받는다.
1 : stdout : Standard Ouput, 표준출력, 특별히 출력이 지정되지 않은 경우 정상적인 출력 결과를 모니터로 출력된다.
2 : stderr : Standard Error, 표준에러, 특별히 출력이 지정되지 않은 경우 에러의 출력 결과를 모니터로 출력된다.

표준입력(0, stdin) -----> SHELL(bash) -----> 표준출력(1, stdout)
(EX: keyboard)              |               (EX: Screen)
				           |
				           V
				 표준에러(2, stderr)
				    (EX: Screen)

쉘에서는 명령어 입력을 특별한 지정이 없는 한 키보드(Keyboad)에서 받는다. 그리고 명령어의 출력 결과를 모니터에 출력해준다. 이러한 명령어의 출력 결과나 입/출력을 리다이렉션 심볼(Redirection Symbol)[<,>,2>]을 사용하여 방향을 재지정 할 수 있다. 
방향을 재 지정함으로 인해 기본 출력 방향이 모니터인 것을 file로 변경이 가능하다. [리눅스는 모든것을 파일로 다룬다.]
EX: cat file1 > file1
        cat file1 > /dev/null 

입력 재지정(입력 리다이렉션, Redirection stdin)
입력 재지정은 표준입력(Stdin)에서 입력을 받지 않고, 다른쪽에서 입력을 받는 것을 입력 재지정 (Redirection stdin)이라고 한다. 원래 특별한 지정이 없다면, 키보드에서 입력을 받지만 다른쪽 방향에서 입력을 받는 것을 말한다. 파일 기술자 0번은 생략 가능하다.

CMD   <  filename (표준입력)
CMD  0<  filename(표준입력 - 파일 기술자 0번)

mail -s "linuxXXX : OK" admin@paran.com < report.txt 
wall <  /etc/hosts

출력 재지정(출력 리다이렉션, Redirection stdout)

출력 재지정은 정상정인 출력 결과를 표준출력(모니터)으로 출력을 하지 않고, 다른 쪽으로 출력하는 것을 출력 재지정(Redirection stdout)이라고 한다. 원래 특별한 지정이 없다면, 모니터로 출력되지만 다른 쪽 방향으로 출력하는 것을 말한다. 파일 기술자 1번은 생략 가능하다.

CMD   >  filename /* 표준출력, 덮어쓰기 기능 */
CMD  1>  filename /* 표준출력, 덮어쓰기 기능 */
CMD   >> filename/* 표준출력, 이어쓰기 기능 */
CMD  1>> filename/* 표준출력, 이어쓰기 기능 */


에러 재지정(에러 리다이렉션, Redirection stderr)

에러 재지정은 표준 에러로 출력하지 않고, 다른 쪽으로 출력하는 것을 에러 재지정(Redirection stderr)이라고 한다. 원래 특별한 지정이 없다면, 모니터로 출력을 하지만 다른 쪽으로 출력하는 것을 말한다. 파일 기술자 2번은 생략 불가능하다. 방향 재지정자를 1번과 동일하게 사용하여 1번 파일 기술자와 구별하기 위해 생략 불가능하다.

CMD  2>  filename/* 표준에러출력 */
CMD  2>> filename /* 표준에러출력 */

# ./script.sh > file.log 2>&1
# ./configure --perfix=/usr/local/apache2 > file.log 2>&1

$ find / -name core –type f 
$ find / -name core –type f 2> /dev/null

# 파이프(PIPE)
- CMD | CMD
파이프(Pipe)란? 앞에 실행한 명령어의 표준출력 결과를 뒤에 실행하는 명령어의 표준입력 값으로 넣어 준다. 앞의 출력 결과를 뒤에 실행하는 명령어의 입력 결과로 보내줌으로 인해 파이프(|)를 사용하면 여러 명령을 동시에 연결하여 사용 할 수가 있다.

ls -al 명령을 사용하게 되면 모든 파일을 보여주므로 한 화면을 넘는 많은 파일을 보여 주게 될 때 "ls -al | more" 를 쓰게 되면 ls 명령에 대한 결과를 more 명령을 통해 확인해 볼 수 있다.

첫 번째 CMD로 출력된 내용을 화면에 그대로 출력하지 않고 파이프 버퍼에 data를 저장 해 두었다가 버퍼에 저장된 결과 값을 가지고 뒤에 있는 CMD의 입력 값으로 넘겨 뒤에 있는 명령어가 실행한 결과를 출력해 주게 된다.
# ps -ef | grep sshd  

배쉬쉘 기능(Bash Shell Funtion)
- bash shell function

# set –o
# set –o vi : 활성화
# set +o vi : 비활성화
# set –o ignoreeof
# set –o noclobber
bash 쉘의 기능
allexport            off
braceexpand          on
emacs                on/* 기본적인 편집기 선언 */
errexit              off
errtrace             off
functrace            off
hashall              on
histexpand           on
history              on
ignoreeof            off/* 로그아웃 방지 기능 선언 */ ^d
interactive-comments on
keyword              off
monitor              on
noclobber            off/* 덮어쓰기 방지 기능 선언 */
# echo 3333 >| test1 : 기능 무시하고 덮어쓰기.
noexec               off
noglob               off
nolog                off
notify               off
nounset              off
onecmd               off
physical             off
pipefail             off
posix                off
privileged           off
verbose              off
vi                   off/* 기본적인 편집기 선언 */
xtrace               off

<Tab> 파일이름 자동 완성 기능

디렉토리에 있는 파일이나 디렉토리의 첫 번째 문자만 입력 후 Tab키를 누르면 첫 글자로 시작하는 파일이나 디렉토리의 나머지 글자가 자동으로 완성된다. 단, 첫 문자가 같은 디렉토리나 파일이 있는 경우 Tab키 한번만 누르면 아무 변화도 없지만 Tab키 두 번 연속으로 누르면 동일하게 시작하는 모든 디렉토리나 파일을 출력해 준다.

[명령어 형식]
# ls t[TAB]

자동완성을 사용하게 되면 실수가 적어진다.
디렉토리 이동, 파일 실행에 있어 좀 더 빠른 작업이 가능.

변수의 종류
지역 변수(Local Variable)
환경 변수(Environment Variable)
특수 변수(Special Variable)

변수 선언 방법
    
# VAR=5 ; export VAR
# echo $VAR
# unset VAR
# export -n VAR

지역변수 선언
변수 선언 방법(EX: bash)
	# VAR=5        (# export VAR=5)
	# export VAR 
	# echo $VAR    (# print $VAR)
	# unset VAR

지역 변수는 현재 사용하고 있는 쉘에만 적용되는 변수 값으로 선언 하는 것이다. 서브 쉘에서 지역 변수 값을 확인하려고 할 경우 값이 나타나지 않는다.
var=5                      echo $var (0)
----------+               +----------
          |               |
          | echo $var (X) |
          +---------------+

환경변수 선언

환경변수는 현재 쉘과 서브쉘에 변수를 모두 적용되는 변수 값으로 선언해 주는 것이다. 환경변수를 사용하는 이유는 이전쉘에서 선언한 변수 값을 서브쉘에서도 동일하게 적용하기 위한 것이다.
export var=hello    echo $var (0)
----------+               +----------
          |               |
          | echo $var (0) |
          +---------------+

set : 모든 변수에 대해 출력 (지역변수 + 환경변수)
env : 환경변수만 출력 

시스템 지역변수
 PS1 명령어 프롬프트에 나타내는 시스템 환경 변수
[root@servera test]#
# echo $PS1
[\u@\h \W]\$

변경된 내용을 영구적인 설정으로 변경하는 방법.

# vi ~/.bashrc 
-------------------------------
.....
#
# Variable Definition
#
export PS1='[\u@\h \w]\$ '
-------------------------------
PS1 시스템 환경 변수 설정하는 방법

쉘프롬프트에 보여지는 항목을 설정하는 것으로 아래와 같은 것들을 사용할 수 있다.
ex) [로그인한 계정@호스트네임 현재경로] #         /* 현재 상태 $ : 프롬프트($, root일 경우 #) */
---------------------------------------
\t     현재 시간을 HH:MM:SS 형식으로 표시
\d     날자를 "요일 월 일" 형식으로 표시 (예, "Tue May 26")
\n     개행문자
\s     쉘의 이름, $0 의 베이스 이름 (마지막 슬래쉬 뒷 부분)
\w     현재 작업 디렉토리
\W     현재 작업 디렉토리의 베이스 이름
\u     현재 사용자의 사용자명
\h     호스트 이름
\#     이 명령의 명령 번호
\!     이 명령의 히스토리 번호
\$     유효 UID가 0 이면 if the effective UID is 0, a #, 그렇지 않으면 $
\nnn   팔진수 nnn에 해당하는 문자
\\     백슬래쉬
\[     비출력 문자의 시퀀스를 시작한다. 프롬프트에 터미널 제어시퀀스를 넣을때 사용한다.
\]     비출력 문자의 시퀀스를 마친다.

PS2 명령어가 아직 끝나지 않았음을 나타낼 때 사용
PS2='> '
# echo $PS2 
>
# ls -a -l 
# ls -a \ 
> -l

시스템 환경변수
# echo $HOME
/root
# cd $HOME 
# pwd
/root
# echo $PWD
/root
# echo $LOGNAME
root
# echo $TERM
xterm
# echo $LANG
ko_KR.UTF-8
# echo $USER
root
# echo $UID
0



알아두면 좋은 특수 변수

① $ : 현재쉘의 PID를 저장하고 있다. 쉘 스크립트내에서 임시 파일의 이름을 지정할 때 보통 사용이 된다.

# echo $$
11991

# ps
PID TTY          TIME CMD
11991 pts/1    00:00:00 bash


② ? : 바로 이전 명령어의 정상 실행 여부에 대한 결과값이 들어 있다. 쉘 스크립트내에서 이전 명령어의 정상 수행 여부를 확인할 때 주로 사용된다.
# ls /nodir
ls: /nodir: No such file or directory
# echo $?
2

③ ! : 바로 이전에 백그라운드로 실행한 프로세스의 PID번호가 저장 된다.

# sleep 300 &
[1] 25192
		[1]   : Job ID
		25192 : PID
# echo $!
25192
# ps
PID TTY          TIME CMD
24868 pts/1    00:00:00 bash
25101 pts/1    00:00:00 bash
25192 pts/1    00:00:00 sleep
25219 pts/1    00:00:00 ps
# sleep 400 &
[2] 25233
# ps
PID TTY          TIME CMD
24868 pts/1    00:00:00 bash
25101 pts/1    00:00:00 bash
25192 pts/1    00:00:00 sleep
25233 pts/1    00:00:00 sleep
25234 pts/1    00:00:00 ps
# echo $!
25233

④ 인자변수(Argument Variable)
$로 표시하여 인자변수의 값이 몇 번째인지 나타낼 수 있다.   인자변수1 | 인자변수2 | 인자변수3
		# ls -a 
		# vi ls 
		----------------------------------
		..... (중략) .....
		echo "$1"
		..... (중략) .....
		----------------------------------

# cd /test 
# vi test.sh
--------------------------------------
		#!/usr/bin/bash
		
		echo $1                     /* 첫번째 인자 */
		echo $2                     /* 두번째 인자 */
		echo $3                     /* 세번째 인자 */
--------------------------------------
# chmod 755 test.sh 
# ls -l test.sh
-rwxr-xr-x 1 root root    38 Aug 19 14:22 test.sh
# ./test.sh test1 test2 test3
test1
test2
test3
# date
Thu Aug 19 14:24:33 KST 2010
# set $(date) 
# echo $1
Thu
# echo $2
Aug
# echo $3
19
# echo $4
14:24:47
# echo $5
KST
# echo $6
2010
# echo $*

 [5] PATH 변수

명령어를 검색할 디렉토리를 선언할 때 사용하여 절대 경로를 쓰지 않고 실행파일명만을 이용해서 사용할 수 있게 한다. which라는 명령도 PATH에 설정되어 있는 디렉토리의 파일의 경로만을 알려준다.

# gedit & 
# which gedit
/usr/bin/gedit
# echo $PATH
/usr/lib/qt-3.3/bin:/usr/kerberos/sbin:/usr/kerberos/bin:/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin:/root/bin:/root/bin
# cd 
# ls -l /test/script.sh
-rwxrwxrwx 1 root root 109  5월 20 15:26 /test/script.sh

(필요하면 아래와 같이 /test/script.sh 스크립트를 생성한다.)


# vi /test/script.sh 
------------------------------
#!/bin/bash

echo "script.sh Execution"
------------------------------
# chmod 755 /test/script.sh

# script.sh
-bash: script.sh: command not found
		(절대경로)# /test/script.sh 
		(상대경로) # cd /test 
		# ./script.sh 
		# script.sh

# vi ~/.bash_profile
# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
        . ~/.bashrc
fi

# User specific environment and startup programs

PATH=$PATH:$HOME/bin
PATH=$PATH:/test                  <----- 라인 추가

export PATH
unset USERNAME

# .  ~/.bash_profile 
# echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/root/bin:/root/bin:/custom
# script.sh
script.sh Excution


[6] HISTTIMEFORMAT 변수
# export LANG=C 
# man bash
------------------------------
/HISTTIMEFORMAT
       HISTTIMEFORMAT
              If this variable is set and not null,  its  value
              is  used  as  a  format string for strftime(3) to
              print the time stamp associated with each history
              entry  displayed by the history builtin.  If this
              variable is set, time stamps are written  to  the
              history  file  so  they  may  be preserved across
              shell sessions.
------------------------------
# man 3 strftime
------------------------------
/%F
       %F     Equivalent  to  %Y-%m-%d  (the ISO 8601 date for-
              mat). (C99)
/%T
       %T     The time in 24-hour notation (%H:%M:%S). (SU)
# vi /etc/profile
..... (중략) .....
for i in /etc/profile.d/*.sh ; do
    if [ -r "$i" ]; then
        if [ "${-#*i}" != "$-" ]; then
            . $i
        else
            . $i >/dev/null 2>&1
        fi
    fi
done

#
# (1) Sfecific Configuration
#
export HISTTIMEFORMAT="%F %T     "

unset i
unset pathmunge
------------------------------
# telnet localhost 
root 사용자로 로그인

# history
..... (중략) ....
  776  10:07:00     clear
  777  10:07:11     clear
  778  10:10:42     clear
  779  10:12:43     export LANG=C
  780  10:13:12     man bash
  781  10:15:13     clear
  782  10:15:16     man 3 strftime
  783  10:17:49     vi /etc/profile
  784  10:20:15     history
# exit 
#
메타캐릭터(Metacharacter)

- ''(작은 따옴표(single quotation))
- ""(큰 따옴표(Double quotation))
- ``(역 따옴표(Back quotation))
- \ (역 슬래쉬(Back slash))
- ; (세미콜론(Semicolon))



■ ''(작은 따옴표(single quotation))

쉘이 해석 할 수 없도록 막아 준다.
# echo $HOME
/root
# echo '$HOME'
$HOME

■ ""(큰 따옴표(Double quotation))

쉘이 해석 할 수 없도록 막아 준다. 단 인식되는 문자($, ``, \)들도 있다.

# echo $HOME 
/root
# echo "$HOME" 
/root
# echo "$HOME is my    directory."
/root is my    directory.

■ ``(역 따옴표(Back quotation))

쉘이 해석할 때 명령어로 인식한다. 따라서 역 따옴표 안의 내용을 실행한다.

# date 
# echo `date`     (# echo $(date))
# echo `hostname` (# echo $(hostname))
# echo "`hostname` is my hostname."

# touch server_`date +%m%d`.log
# tar cvzf /backup/home_`date +%m%d`.tar.gz /home
# while true
> do
> echo "------------`date`---------"
......
> sleep 2
> done

■ \ (역 슬래쉬(Back slash))

바로 이후에 있는 문자을 쉘이 해석 할 수 없도록 막아 준다.

# echo $HOME
# echo \$HOME    (# echo '$HOME')

# find / -name core -type f -exec rm -f {} \;
# find / \( -perm -4000 -o -perm -2000 \) -type f

		[참고] \(역 슬래쉬)
		# \CMD
		# CMD\
		
		# ./configure --prefix=/usr/local/apache2 --options2=.... --options3=.... .....
		# ./configure —prefix=/usr/local/apache2 \
		> --options2=.... \
		> --options3=.... \
		> --options4=....

■ ; (세미콜론(Semicolon))

한개의 라인에 여러개 명령어 수행할 때 사용한다.

# ls 
# date 
# cal 

# ls ; date ; cal 

# alias pps='ps -ef | head -1 ; ps -ef | grep $1'
# EDITOR=/usr/bin/vim ; export EDITOR    (# export EDITOR=/usr/bin/vim)

# CMD1 ; CMD2 는 사실 위험하다.
CMD1이 실패한 경우 CMD2가 현재 디렉토리에서 실행된다.
삭제, 이동..등의 명령을 ; 을 이용하여 사용하면 위험하다.

# CMD1 && CMD2
CMD1이 $? = 0 인경우에만 CMD2가 실행

# CMD1 || CMD2
CMD1이 $? != 0 인경우에만 CMD2가 실행

# 히스토리(HISTORY)
# history 
지금까지 사용한 명령어의 기록을 보는 용도
History 관련 명령어
     # history
     # history 5
     # cat ~/.bash_history
     # ![history_number]

# crtl + r : 역방향으로 직전에 사용한 명령어를 확인 가능
# 방향키로 이동. 

사용자가 로그인 하게 되면 사용자의 명령어를 저장하기 위해서 Stack 공간이 할당된다. Stack 공간은 ksh 쉘을 사용하고 있다면, 기본값은 1000개의 명령어를 저장할 수 있는 공간이 할당된다. bash 쉘은 1000개의 명령어를 저장 할 수 있다. 또한 sh 쉘은 히스토리 기능이 없다.

HISTSIZE              /* 히스토리를 기록하는 스택의 크기를 지정 */
HISTFILE              /* 히스토리 내용을 지속적으로 저장하는 파일 이름 지정 */
                      /* 기본값: ~/.bash_history */
HISTFILESIZE          /* 히스토리파일의 크기 지정 */


 alias

명령어가 실행되는 기준.
hash , alias / path


# 명령어를 실행 ls 
1. alias 에서 ls로 시작하는 명령어 있는지 확인
alias ls='ls --color=auto'
2. hash에서 ls 명령에 대한 기록이 있는지 확인
있으면 바로 hash기록 + alias의 옵션까지 더해서 사용
없다면 PATH로 이동
3. PATH 에서의 동작
첫번째 디렉토리부터 하나씩 검색
DIR1:DIR2:DIR3...>
첫번재 걸리는 ls를 실행
hash에 사용한 명령어 대한 절대경로를 메모리에 로드

기존에 존재하는 명령에는 옵션이 상당수 있다.
이 옵션중에 기본적으로 사용해야 하는 옵션들을 미리 등록을 해두고 어렵지 않게 쉽게 쓸수 있다.

alias의 문제  -> ; 
cd 명령어와 다른 명령어를 한 줄 에서 처리하려고 하면 오동작
요런 경우에 사용하는 것이 function

.bashrc
cd 명령어 자체를 function을 이용하여 기능으로 전환
function cd { builtin cd $* && pwd ; }

- 사용자 환경 파일 개요
- 사용자 환경파일 읽혀 지는 순서
    
    로그인시 읽혀 지는 환경파일
    
    쉘이 실행 될때 마다 읽혀 지는 환경파일
    
    로그아웃 할 때 마다 읽혀 지는 환경파일
    
- 사용자 환경파일 분석
- 사용자 환경파일 활용

---

사용자 환경 파일(Bash Initialization)

 [1] 환경 파일이 읽혀 지는 순서

 [1-1] 로그인시에 읽혀 지는 환경 파일

■ /etc/profile
	■ /etc/profile.d/*.sh

■ ~/.bash_profile (~/.bash_profile -> 파일이 없으면 -> ~/.bash_login -> 파일이 없으면 -> ~/.profile)
	■ ~/.bashrc
		■ /etc/bashrc


[1-2] 쉘이 실행 될때 마다 읽혀 지는 환경 파일

■ ~/.bashrc
	■ /etc/bashrc
		■ /etc/profile.d/*.sh

[1-3] 로그아웃 할 때 마다 읽혀 지는 환경 파일

■ ~/.bash_logout

검색

grep : 출력물에서 패턴을 검색하여 해당 라인을 출력

find : inode 정보에 기반한 검색 


grep 명령어
    
     # grep OPTIONS PATTERN ARG1
    
    OPTIONS : -l, -n, -I, -v, -w
    
    PATTERN : * . ^root root$ [abv] [^a]

파일 내에서 특정한 패턴을 검색하여 그 패턴을 포함하는 모든 줄을 화면에 출력하는 명령어. 파일 내에 특정한 패턴을 찾기 위해 많이 사용한다. 찾으려고 하는 패턴을 정규식(g/re/p, Globally/Regular Expression/Print)이라고 표현한다. grep 명령어의 약자에서 볼 수 있듯이 많은 패턴을 제공하고 있다.

(옵션 사용법) "# grep OPTIONS PATTERN file1"
# grep -l root /etc/hosts /etc/passwd /etc/group /* 여러 파일 중 검색 문자열 존재 파일 출력 */
# grep -n root /etc/group  /* -n: number line, 파일에서 root 문자열을 검색하고 라인 번호도 같이 출력 # cat -n /etc/passwd | grep root */ 
# grep -v root /etc/passwd  /* -v: inverse, 파일에서 검색 문자열을 제외하고 나머지 출력 */
# grep -i root /etc/passwd  /* -i: ignore case, 파일에서 검색 문자열의 대소문자를 구분하지 않음 */
# grep -w root file1        /* -w: word , 단어의 일부분이 아닌 단어의 전체가 일치하는 경우 출력 */
# grep --color root /etc/passwd

옵션 : 설명

-l : (-l : list files) 패턴이 있는 파일이름만을 출력한다.
-n : (-n : number line) 패턴을 포함하는 줄을 출력할 때 줄번호와 함께 출력한다.
-v : (-v : inVerse, except) 패턴을 포함하는 줄을 제외하고 출력한다.
-c : (-c : count) 패턴을 찾은 줄의 수를 출력한다.
-i : (-i : ignore case, 대문자/소문자) 패턴을 찾을 때 대소문자를 구분하지 않는다.

(패턴 사용법) "# grep OPTIONS PATTERN file1"

*       # grep 'ro*t' /etc/passwd       모든문자를 포함(영문자,숫자) 
.       # grep 'no...y' /etc/passwd     (.에 하나의 영문자나 숫자를 치환)
^root   # grep '^root' /etc/passwd      라인의 시작이 root인 라인을 검색
root$   # grep 'root$' /etc/group       라인의 끝이 root로 끝나는 라인을 검색
[abc]   # grep 'user0[123]' /etc/passwd user0으로 시작하여 1이나 2나,3으로 끝나는 문자열

파일내의 특정 패턴 여러개 검색하기
grep의 태생적 한계 -> 하나이상의 패턴 검색이 불가능

# cat /etc/passwd | egrep '(root|team01)'
# cat /etc/passwd | egrep 'root|team01'
# cat /etc/passwd | grep -E 'root|team01'

 fgrep(Fixed grep) CMD
# fgrep '^root' file1

# cat file1 | fgrep *linux
# cat file1 | grep -F *linux

파일 명으로만 검색하는 방법
# updatedb ; locate 패턴


find


# find / -name core –type f

 # find / -user user-1 –group class1

 # find / -mtime [-7|7|+7]

 # find / -perm [755|-755]

 # find / -size [-300M|300M|+300M]

 # find / -name core –type f –exec rm –f {} \;

 # find / -name core –type -ls

 # find / -name core –type f –ok rm {} \;

 # find /Log_Dir –name "*.log" –type f \

 > -mtime +30 –exec rm 0rf {} \;

 # find /Log_Dir –mtime -2 –size +1G –type

디렉토리안에서 원하는 파일을 찾고자 할 때 사용하는 명령어이다. find 명령 다음에 시작 디렉토리를 정해 주고 찾고자하는 파일 이름 앞에 옵션을 주면 된다.

[명령어 형식]
# find [검색시작위치] [옵션 : 설명
-name : 파일 이름을 기준으로 검색
-iname: 대소문자를 구별하지 않고 파일 이름을 기준으로 검색
-perm : 파일 권한을 기준으로 검색한다.

-type : 파일의 종류를 기준으로 검색
b : 블록 파일
c : 문자
d : 디렉토리
f : 파일
l : 링크
s : 소켓
-size : 파일의 크기를 기준으로 검색
+n : n보다 크다
-n : n보다 작다
n : n이다
b : 512-byte
c : Bytes (Character = Byte)
k : Kilo Byte
M : Mega Bytes
G : Giga Bytes
w : 2-byte
-links : 링크의 개수를 기준으로 검색
-user : 사용자 ID를 기준으로 검색
-group: 그룹 ID를 기준으로 검색
-atime : 특정 기간 이상 접근하지 않은 파일을 기준으로 검색
-mtime : 특정 기간 이상 수정되지 않은 파일을 기준으로 검색
-inum : number 지정된 inode 번호와 파일을 찾는다.
-print : 표준출력으로 검색된 파일 출력: GNU는 디펄트, Unix는 필수 입력
-exec command {} ₩; :찾은 각 파일에 대해 지정된 명령을 실행
-ok command {} \; : 실행여부(실행 되어 있는지 아닌지)를 사용자에게 확인 후 명령을 실행옵션1] [인자값1] [옵션2] [인자값2] ...

# find . -name file1 -type f -exec rm -fv {} \; 


find 명령어에서 -exec 옵션에서 중괄호 {}의 역할
find 명령어는 검색으로 출력되는 하나 이상의 파일 및 디렉토리 결과를 가지고 있다. 이 결과를 바탕으로 -exec 옵션에서는 2차적으로 명령을 진행가능하다.
이때 [}는 버퍼의 역할을 하며 순차적으로 처음 검색된 내용부터 차례대로 치환된다. 이 치환은 마지막 검색내용이 입력될때까지 반복된다.

find 명령어에서 -exec 옵션에서 세미콜론(;)의 역할
find 명령어를 사용하면서 출력된 결과를 가지고 바로 후처리 실행으로 이어질수 있는 옵션인 exec ok가 존재한다. 해당 문법의마지막에는 세미콜론(;)이 입력된다. 하지만 쉘에서는 세미콜론(;)을 기준으로 앞 명령어와 뒤 명령어를 구분하는 구분자로 사용하기 때문에 그냥 사용할수는 없으며 바로 앞에 역슬래시를 붙여 \; 구조로 만들어줘야 find 명령어에서 정상적으로 해석가능하다.

프로세스

프로그램 : DISK에 저장되어 있는 실행가능한 상태의 파일
프로세스 : 프로그램을 실행하여 메모리에 적재된 상태.

ps top 
공통점 : /proc 디렉토리의 정보에 기반
차이점 : 
ps: 순간 캡처된 데이터로 리소스에 대하여 출력
top: 실시간으로 캡처를 연속하여 리소스에 대하여 출력, 옵션을 이용하여 정렬 기준을 변경 하거나 특정 PID 번호만 확인하는 것이 가능.

비슷한 프로그램이 시중에 많음.
glances 

쉘에서 옵션없이 ps 를 사용한 경우
# ps
    PID TTY          TIME CMD
   1083 pts/0    00:00:00 bash
   1316 pts/0    00:00:00 ps

# ps -f
UID          PID    PPID  C STIME TTY          TIME CMD
root        1083    1082  0 20:08 pts/0    00:00:00 -bash
root        1317    1083  0 20:33 pts/0    00:00:00 ps -f

# ps -ef | head -1
UID          PID    PPID  C STIME TTY          TIME CMD

(ps -ef 명령어 출력 결과 해석)
----------------------------------------------------------------------------
UID,   프로세스를 실행시킨 사용자, 프로세스의 주인
PID,   프로세스 아이디, 프로세스 식별 번호
PPID,  부모 프로세스 아이디
C,     현재 사용되지 않는 필드
STIME(Start Time), 프로세스 실행 시작 시간
TTY,   제어 터미널, 프로세스가 실행된 터미널    
TIME,  CPU 사용 누적 시간
CMD,   명령어(옵션 + 인자 포함)
----------------------------------------------------------------------------
ps aux | head -1
USER         PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND

user는 프로세스 소유자의 이름, ( u 옵션 )
PID는 프로세스 식별자 번호
%CPU : 마지막 1분 동안 프로세스가 사용한 CPU 점유율 ( u 옵션 )
%MEN : 마지막 1분 동안 프로세스가 사용한 메모리의 점유율 ( u,v 옵션 )
       -u 옵션으로 사용자의 ID나 이름을 지정하여 지정한 사용자의 프로세스만을 살펴 볼수 있다.
VSZ : 가상 메모리에 적재된 프로세스의 kb단위 크기
START는 프로세스가 시작된 시간

STAT 필드의 상태
-D : 디스크 입출력 대기 상태로 interrupts를 걸 수 없는 상태
-R : 실행중
-S : 짧은 sleep 상태
-T : 정지 상태
-Z : 좀비 상태
-W : 상주한 페이지가 없는 프로세스
-< : 높은 우선권 프로세스
-N : 낮은 우선권 프로세스
-L : 페이지가 락이 걸린 상태

top

# uptime
 20:44:02 up  1:06,  1 user,  load average: 2.00, 0.00, 0.00
load average : 1 5 15분의 부하 평균
* CPU 싱글코어 기준(load avg 예시):
1.0 -> 100%
0.5 -> 50%
1.7 -> 170%


200% , 000% , 000%

200% 

시스템 부하평균 = 지수이동 평균값

부하평균값은 표시대는 그대로 보는것이 아니라 cpu 개수로 나누어야 한다.
Task 프로세스 상태:

total: 전체 프로세스
running: 실행 중 프로세스
sleeping: I/O 나 event 기다리는 프로세스
stopped: 컨트롤 + z 와같은 STOP 시그널 받은 프로세스
zombie: 프로세스가 종료되었지만 OS 내부 시스템 자원
              해지가 안된 프로세스
CPU 사용율 %:

us: user-mode (높은 우선순위 nice 0또는 음수)
sy: kernel-mode
ni: user-mode (낮은 우선순위 nice 양수)
id: idle (시스템 얼마나 여유롭나 ?)
wa: I/O wait (처리완료를 기다린 시간)
hi: hard irq (인터럽트 전반부처리(빠른처리))
si: soft irq (인터럽트 후반부처리(지연처리))
st: 가상 CPU 구동 (Guest OS를 위한 VM 처리)

MEM 사용율 % (free 명령 동일결과)

total: 총 메모리 공간 KB
free: 남는 메모리 공간 KB
used: 사용중인 메모리 공간 KB
buff/cache: 디스크 데이터을 메모리에 캐시 KB
available: 프로세스가 처음시작시 사용가능한 메모리공간 KB
             (회수하면 얻을수있는 공간 포함)
swap: 메모리의 내용을 저장해둘수있는 Disk 공간 KB

PID: 프로세스 ID
PR: 20 + nice
NI: nice (친절도)
VIRT: 가상메모리공간
RES: 실제 사용중인 물리메모리 공간
SHR: 공유메모리 공간
S: 프로세스 상태 (STATE)
    'D' = uninterruptible sleep (예시: 디스크I/O처리대기)
    'R' = running
    'S' = sleeping (예시: 네트워크 I/O 처리대기)
    'I' = idle (예시: sleep 중인 kernel thread)
    'T' = stopped by job control signal
    't' = stopped by debugger during trace
    'Z' = zombie
%CPU: CPU 사용율
%MEM: 메모리 사용율
TIME+: 프로그램 실행한시간

포그라운드 / 백그라운드

포그라운드  : 쉘을 점유한 상태로 프로세스가 실행
-------------
shell
-------------
백그라운드 : 쉘을 점유하지 않은 상태로 프로세스가 실행

# sleep 60
# sleep  60 &

백그라운드 -> 포그라운드 -> 백그라운드

# sleep  600 &
[1]sleep 600

# jobs   : 백그라운드 프로세스를 확인하기 위한 명령
[1]   sleep 600
작업 번호와 명령어가 무엇인지 확인

# fg 1
백그라운드에 존재하는 작업중에 1번을 포그라운드로 전환

# ctrl + z : 현재 포그라운드에 있는 작업을 백그라운드로 전환
* 백그라운드로 전환된 프로세스를 일단 멈춰있기 때문에 다시 실행 시켜줘야 한다.
# bg 1  : 해당번호의 작업을 재실행



* 해당 쉘을 빠져나오게 되면 백그라운드 작업을 제어할 수 없기 때문에 나오기전에 확인 필수 !

https://joonyon.tistory.com/entry/%EC%89%BD%EA%B2%8C-%EC%84%A4%EB%AA%85%ED%95%9C-nohup-%EA%B3%BC-%EB%B0%B1%EA%B7%B8%EB%9D%BC%EC%9A%B4%EB%93%9C-%EB%AA%85%EB%A0%B9%EC%96%B4-%EC%82%AC%EC%9A%A9%EB%B2%95


# kill -l 전체 목록
# kill PID
# kill -9  PID
15 /  9 차이점
메모리 클린업  

USB

win  USB3.0

USB 연결 > 자료 복사 
보이는 화면에서는 종료된것 처럼 처리
백그라운드 >> 복사

하드웨어 안전하게 제거
 USB를 해제하는데 필요한 모든 태스크를 정상절차로 실행

https://www.baeldung.com/linux/get-cpu-usage













